# KeyCapture

Open source repository for capturing keystrokes.

Runs locally on your personal system. Generates a text file locally containing keys (with some timestamps, by default timestamped at each keystrokes separated by more than 30 seconds from an earlier timestamp).

Can be paused. Can be stopped. Can be deleted. Does no communication with the internet. Open-source so all checkable in this repository.
